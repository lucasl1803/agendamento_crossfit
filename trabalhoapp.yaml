name: agenda_crossfit
description: Aplicativo de agendamento de aulas de Crossfit

publish_to: 'none'

environment:
  sdk: '^3.5.0' 
  
dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5 
  intl: ^0.18.0 

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/

